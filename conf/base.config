/*
~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
    JaneliaSciComp/easifish Nextflow base config file
~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
    Runs in `local` mode - all jobs will be run on the logged in environment.
----------------------------------------------------------------------------------------
*/

process {

    // Defaults for all processes
    cpus   = { ParamUtils.check_max( 1    * task.attempt, 'cpus'   ) }
    memory = { ParamUtils.check_max( 6.GB * task.attempt, 'memory' ) }
    time   = { ParamUtils.check_max( 4.h  * task.attempt, 'time'   ) }

    errorStrategy = { task.exitStatus in ((130..145) + 104) ? 'retry' : 'finish' }
    maxRetries    = 1
    maxErrors     = '-1'

    // Process-specific resource requirements
    withLabel:process_single {
        cpus   = 1
        memory = { ParamUtils.check_max( 6.GB * task.attempt, 'memory'  ) }
        time   = { ParamUtils.check_max( 4.h  * task.attempt, 'time'    ) }
    }
    withLabel:process_low {
        cpus   = { ParamUtils.check_max( 2     * task.attempt, 'cpus'    ) }
        memory = { ParamUtils.check_max( 12.GB * task.attempt, 'memory'  ) }
        time   = { ParamUtils.check_max( 4.h   * task.attempt, 'time'    ) }
    }
    withLabel:process_medium {
        cpus   = { ParamUtils.check_max( 6     * task.attempt, 'cpus'    ) }
        memory = { ParamUtils.check_max( 36.GB * task.attempt, 'memory'  ) }
        time   = { ParamUtils.check_max( 8.h   * task.attempt, 'time'    ) }
    }
    withLabel:process_high {
        cpus   = { ParamUtils.check_max( 12    * task.attempt, 'cpus'    ) }
        memory = { ParamUtils.check_max( 72.GB * task.attempt, 'memory'  ) }
        time   = { ParamUtils.check_max( 16.h  * task.attempt, 'time'    ) }
    }
    withLabel:process_long {
        time   = { ParamUtils.check_max( 24.h  * task.attempt, 'time'    ) }
    }
    withLabel:process_high_memory {
        memory = { ParamUtils.check_max( 200.GB * task.attempt, 'memory' ) }
    }
    withLabel:error_ignore {
        errorStrategy = 'ignore'
    }
    withLabel:error_retry {
        errorStrategy = 'retry'
        maxRetries    = 2
    }
}
